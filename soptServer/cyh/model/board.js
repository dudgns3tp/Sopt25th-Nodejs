const authUtil = require('../module/authUtil');
const statusCode = require('../module/statusCode');
const responseMessage = require('../module/responseMessage');
const userDB = [];

module.exports = {
    create: ()=>{ //signin signup 메소드가 비동기로 동작
        return new Promise((resolve,reject)=>{ 
        
        })

    },
    read: (id,pwd)=>{ 
        return new Promise((resolve,reject)=>{ 
        
        })
    },
    readAll: (id,pwd)=>{ 
        return new Promise((resolve,reject)=>{ 
        
        })
    },
    update: (id,pwd)=>{ 
        return new Promise((resolve,reject)=>{ 
        
        })
    },
    delete: (id,pwd)=>{ 
        return new Promise((resolve,reject)=>{ 
        
        })
    }
}